% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/si_product.R
\name{si_product}
\alias{si_product}
\title{Resolve one simplified-issue product's configuration}
\usage{
si_product(si_type, rulebook)
}
\arguments{
\item{si_type}{Product code, e.g. \code{"325"} or \code{"3105"}.}

\item{rulebook}{A rulebook from \code{\link[=load_si_rulebook]{load_si_rulebook()}}.}
}
\value{
A named list: \code{si_type}; the four window months (\code{medical_advice_mon},
\code{medical_advice_lookback_mon}, \code{inpatient_surgery_mon},
\code{critical_disease_mon}); \code{critical_disease_set}; \code{coverages}, the coverages
the product actually sells (read off the rule sheet, so 3105's absent \code{care}
coverage cannot be mis-stated); a \code{critical_disease_window(coverage)} closure
giving each coverage its window; and \code{critical_disease}, the product's slice
of the critical-disease table.
}
\description{
Pulls a single product's windows and coverage set out of a rulebook. A product
code's digits are its three application-form windows -- 325 is 3 months
medical-advice, 2 years inpatient-surgery, 5 years critical-disease -- but the
codes are not machine-parseable, so the \code{product} sheet states each window
explicitly and this reads them off it.
}
\seealso{
\code{\link[=load_si_rulebook]{load_si_rulebook()}}, \code{\link[=match_si_rule]{match_si_rule()}}.
}
