% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_decision.R
\name{combine_decision}
\alias{combine_decision}
\title{Combine per-disease decisions into a per-insured final decision}
\usage{
combine_decision(
  applied,
  rulebook,
  decision_cols = attr(applied, "decision_cols")
)
}
\arguments{
\item{applied}{The per-disease decisions from \code{\link[=match_rule]{match_rule()}} (\verb{$applied}).}

\item{rulebook}{A named list of the four config tables the composition reads:
\code{decision}, \code{exclusion}, \code{reduction}, \code{loading}. Passing them as one object
parallels the simplified-issue \code{\link[=combine_si_decision]{combine_si_decision()}} and keeps the caller
from spelling four arguments in a fixed order.
\itemize{
\item \code{decision} -- decision-code table with columns \code{code}, \code{priority}
(lower = worse; ranks the two terminal codes and the codes merging by
\code{priority}, nothing else), \code{combiner}
(\code{priority}/\code{exclusion}/\code{loading}/\code{reduction}, the within-class merge rule),
\code{role} (marks the engine-emitted codes: \code{standard}, \code{decline},
\code{underwriter} -- an \code{underwriter} row is required, since unmatched diseases
are referred there), \code{auto} (\code{1}/\code{0}, read by \code{\link[=tabulate_decision]{tabulate_decision()}} and
\code{plot()} to flag which codes count as automatic), and \code{max_sites} (the
distinct exclusion sites a coverage tolerates before it declines; required
on the exclusion code's row, blank on every other, as \code{role} is). Its row
order is the order the composed codes are written in.
\item \code{exclusion}, \code{reduction} -- period-code tables listing the valid \code{mark}s
(\code{"5i"} = 5 years minus elapsed, \code{"3"} = 3 years, \code{"99"} = whole period);
the period logic is parsed from the mark itself.
\item \code{loading} -- columns \code{at_least}, \code{decision}. A staircase over the summed
loading index: a row claims every sum from its \code{at_least} (inclusive) up to
the next row's, the last row runs to infinity, and the first must start at
\code{0}. A \code{decision} holding the bare loading letter keeps the loading itself,
so the band reads \code{E(75)}; any other value substitutes for it, so \code{"U"} or
\code{"D"} escalates the coverage rather than loading it.
}}

\item{decision_cols}{Coverage decision columns (default: the \code{"decision_cols"}
attribute set by \code{\link[=match_rule]{match_rule()}}).}
}
\value{
A wide \code{data.table}, one row per \code{id}, one column per coverage. The
four supplied tables ride along as attributes (\code{decision_table},
\code{exclusion_table}, \code{reduction_table}, \code{loading_table}), together with
\code{decision_cols} (the rule-set coverage order, which the \code{id ~ coverage}
reshape would otherwise sort away), so downstream summaries such as
\code{\link[=tabulate_decision]{tabulate_decision()}} and \code{plot()}, and the functions that recombine
(\code{\link[=trace_decision]{trace_decision()}}, \code{\link[=relax_rule]{relax_rule()}}), can recover them without re-passing.
The \code{"unresolved"} attribute holds one row per decision code the config tables
could not resolve -- \code{code}, \code{reason}, the \code{rule_no} and \code{kcd_main} that wrote
it, and the \code{n_id} / \code{n_cell} it reached -- or \code{NULL} when every code resolved.
}
\description{
Collapses the per-disease decisions from \code{\link[=match_rule]{match_rule()}} to one decision per
\verb{(id, coverage)}. Fully table-driven and code-letter agnostic: no decision code
(\code{R}/\code{E}/\code{L}/\code{S}/\code{D}/\code{U} ...) is hard-wired. \code{decision_table} supplies both the
class letters and their meaning, so a company that writes exclusion as \code{X}
instead of \code{R} only edits its table -- the code is unchanged.
}
\details{
Two columns of \code{decision_table} do two different jobs. \code{combiner} says how
several diseases' codes of \emph{one} class merge into that class's single result:
\code{exclusion} unions the sites and keeps the longest period per site, \code{loading}
sums the indices, \code{reduction} keeps the longest period, and \code{priority} -- for a
class with no merge rule of its own -- keeps the worst code.

Two of those merges leave behind an amount the rule set then bands. The
exclusion counts distinct sites and declines a coverage once they exceed the
\code{max_sites} its \code{decision_table} row carries. The loading sums its indices and
looks the sum up in \code{loading_table}: the band it lands in either keeps the
loading the class wrote for itself, when its \code{decision} is the bare loading
letter, or substitutes another code -- \code{E} at \code{at_least = 0} writes \code{E(75)},
while \code{U} at \code{at_least = 50} refers that sum to the underwriter instead.

\code{role} then says how the classes meet each other. The \code{decline} and
\code{underwriter} codes are terminal: either one stands alone on a cell, suppressing
every restriction, and the worse of the two wins. The \code{standard} code is the
identity and drops out. Everything else composes, so an insured can carry an
exclusion, a loading and a reduction on one coverage at once
(\code{"E(75),L(36),R03(24),R12(24)"}), written in the decision table's row order.
Terminality is judged on each combiner's \emph{output}: a summed loading that reaches
a decline band, or an exclusion whose site count does, escalates the whole cell
even though neither input code was terminal.

One consequence is worth knowing before writing the table. Every class whose
\code{combiner} is \code{priority} shares a single bucket, so those classes stay exclusive
of \emph{each other} -- a cell holding both \code{C} and \code{M} keeps only the worse of the
two, and the loser never reaches the composition. That is the right reading when
the two are genuine alternatives. When two such restrictions must instead sit on
one cell together, give each its own \code{combiner}, so each contributes a result of
its own for the composition to pick up.

\code{priority} therefore carries less weight than its name suggests. It orders the
two terminal codes against each other, and it ranks the codes that merge by
\code{priority} within their bucket. It does \emph{not} order the composition -- that
follows the decision table's row order -- and it does not decide which classes
compose, which is \code{role}'s job. Codes that compose may share a priority number
freely; doing so states that none of them dominates the others.

A decision code nothing can read -- a class letter absent from \code{decision_table}, a
malformed exclusion token, a period mark absent from its table, a loading with no
numeric index -- refers its coverage to the underwriter, and never quietly stands
the coverage instead. Expiry and unreadability look alike once a code has been
dropped, so the whole code vocabulary is judged before any merging: an exclusion
that ran out leaves the coverage standard, while one nobody can read escalates it.
Every such code is counted in the \code{"unresolved"} attribute and reported in a single
warning naming the rule rows that wrote it, so a rule-set typo surfaces instead of
turning into a silent automatic acceptance.

Every insured reaches here with at least one row in \code{applied} -- one with
nothing to underwrite arrives on the no-diagnosis code, whose decision the
rule set supplies -- so every id gets a decision and none has to be re-added
afterwards. \code{nrow(combined)} therefore equals the number of insured in the
claim feed.
}
