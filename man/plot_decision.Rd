% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_decision.R
\name{plot_decision}
\alias{plot_decision}
\title{Plot the decision composition per coverage}
\usage{
plot_decision(
  final,
  group = c("auto", "category"),
  order = c("column", "auto_high", "auto_low"),
  min_label = 0.03,
  title = "decision composition per coverage"
)
}
\arguments{
\item{final}{A wide final-decision table from \code{\link[=combine_decision]{combine_decision()}}.}

\item{group}{Column to stack and colour by: \code{"auto"} (default) or
\code{"category"}.}

\item{order}{Coverage order along the x-axis: \code{"column"} (default, the
coverage column order defined in the rule set, from \code{final}'s
\code{decision_cols} attribute), \code{"auto_high"} (highest auto-decided share
first), or \code{"auto_low"} (lowest first).}

\item{min_label}{Segments whose share is at or below this are left unlabelled,
to keep thin slivers from cluttering (default \code{0.03}).}

\item{title}{Plot title (default \code{"decision composition per coverage"}).}
}
\value{
A \code{ggplot} object. When \code{group = "auto"}, auto-decided (\code{1}) is blue
and manual review (\code{0}) is red.
}
\description{
A stacked bar of each coverage's decision composition from
\code{\link[=tabulate_decision]{tabulate_decision()}} -- by default the auto-decided vs manual-review share --
with each segment's percentage labelled at its centre.
}
\seealso{
\code{\link[=tabulate_decision]{tabulate_decision()}}.
}
