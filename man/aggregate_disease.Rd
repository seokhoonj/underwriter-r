% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_disease.R
\name{aggregate_disease}
\alias{aggregate_disease}
\title{Aggregate mapped diagnoses to per-insured underwriting inputs}
\usage{
aggregate_disease(disease_long)
}
\arguments{
\item{disease_long}{A long, mapped table from \code{\link[=map_disease]{map_disease()}}.}
}
\value{
A \code{data.table}, one row per \verb{(id, kcd_main)}, with \code{age}, \code{hos_day},
\code{sur_cnt}, \code{out_cnt}, \code{hos_elp_day}, \code{sur_elp_day}, \code{out_elp_day}, \code{elp_day}.
}
\description{
Collapses the long, mapped table to one row per \verb{(id, kcd_main)} with the
inputs a rule set bands on. Reviewed rows only (\code{review == 1}). Counts use a
fixed 5-year window; elapsed days use each disease's lookback window (with the
5-year window as the fallback for unmapped \code{"ZZZ"} codes, which are kept so
they can route to manual review).
}
\details{
Elapsed days are computed per treatment type -- \code{hos_elp_day} (hospitalization),
\code{sur_elp_day} (surgery), \code{out_elp_day} (outpatient) -- because products combine
them differently (general insurance uses the minimum of all three, which is
returned as \code{elp_day}; simplified-issue questions use other subsets). A line
that is both a hospitalization and a surgery feeds both. \code{hos_day} is the
dedup-union of hospital calendar days (\code{\link[instead:count_stay]{instead::count_stay()}}); \code{sur_cnt} and
\code{out_cnt} count distinct accident dates.
}
