% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_si_decision.R
\name{trace_si_decision}
\alias{trace_si_decision}
\title{Trace one insured's simplified-issue decision back to the three questions}
\usage{
trace_si_decision(
  mapped,
  id,
  rulebook,
  product,
  source = c("icis", "declaration")
)
}
\arguments{
\item{mapped}{Mapped claim lines from \code{\link[=map_disease]{map_disease()}}.}

\item{id}{The single insured id to trace.}

\item{rulebook}{A rulebook from \code{\link[=load_si_rulebook]{load_si_rulebook()}}.}

\item{product}{Product configuration from \code{\link[=si_product]{si_product()}}.}

\item{source}{\code{"icis"} or \code{"declaration"}, as in \code{\link[=match_si_rule]{match_si_rule()}}.}
}
\value{
A \code{data.table}, one row per coverage: the final \code{dec} and \code{reason}, the
\code{question} that decided it, the driving \code{kcd_main}, and \code{answers} -- every
question's answer for that coverage as \code{"question:dec"}, \code{" | "}-separated --
so the fold is visible.
}
\description{
The simplified-issue counterpart of \code{\link[=trace_decision]{trace_decision()}}. For a single insured
it runs the engine over that insured's claims alone and shows, per coverage,
what each of the three questions answered and which one decided the coverage.
Use it to audit one case: what fed in, how it combined, and why the coverage
landed where it did.
}
\details{
Like \code{\link[=trace_decision]{trace_decision()}}, it reuses the engine (\code{\link[=match_si_rule]{match_si_rule()}} /
\code{\link[=combine_si_decision]{combine_si_decision()}}) rather than reimplementing the logic, so a trace can
never silently disagree with a production run. An independent, spec-derived
recomputation used to cross-check the engine lives in the test suite, not here.
}
\seealso{
\code{\link[=trace_decision]{trace_decision()}}, \code{\link[=match_si_rule]{match_si_rule()}}, \code{\link[=combine_si_decision]{combine_si_decision()}}.
}
