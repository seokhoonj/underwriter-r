% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kcd.R
\name{split_kcd}
\alias{split_kcd}
\title{Split a multi-code cell into normalized codes}
\usage{
split_kcd(cell, len = 4L)
}
\arguments{
\item{cell}{A single diagnosis-code cell (length-1 character).}

\item{len}{Integer length to roll each flat code to (default \code{4L}).}
}
\value{
A character vector of the cell's normalized codes, \code{NA} entries
dropped.
}
\description{
Like \code{\link[=normalize_kcd]{normalize_kcd()}} but keeps every comma-separated code in the cell, not
just the first. Used when a single cell holds several diagnoses that should be
redistributed (first -> main diagnosis, the rest -> sub-diagnoses).
}
\examples{
split_kcd("K63.5, S33, junk")
}
