% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_ruleset.R
\name{diagnose_ruleset}
\alias{diagnose_ruleset}
\title{Diagnose authoring defects in a rule set}
\usage{
diagnose_ruleset(
  ruleset,
  decision_cols = setdiff(names(ruleset), .NON_DECISION_COLS),
  verbose = TRUE
)
}
\arguments{
\item{ruleset}{A rule-set \code{data.table} (or coercible), the same table
\code{\link[=match_rule]{match_rule()}} consumes.}

\item{decision_cols}{The coverage decision columns; by default every column
not in the fixed non-decision set (\code{.NON_DECISION_COLS}). Pass explicitly
when the rule set carries extra attribute columns, exactly as for
\code{\link[=match_rule]{match_rule()}}.}

\item{verbose}{If \code{TRUE} (default) print a report; the list is always returned
invisibly.}
}
\value{
Invisibly, a named list with \code{n_rule}, \code{n_kcd}, \code{n_auto} (the
\code{decl_yn == 0} row count) and the four sections above.
}
\description{
Statically validates a rule set -- no claim data required -- for the authoring
errors that \code{\link[=match_rule]{match_rule()}} can only surface at run time, and only once some
input happens to land in the offending band. A rule whose bands are never hit
by the data at hand looks clean to \code{\link[=match_rule]{match_rule()}} yet is still defective; this
catches it up front.
}
\details{
Only \code{decl_yn == 0} rows are examined, since those are the rows \code{\link[=match_rule]{match_rule()}}
applies. The band overlap and duplicate tests use the four bands
\code{\link[=match_rule]{match_rule()}} joins on (\code{age}, \code{elp_day}, \code{sur_cnt}, \code{hos_day}); \code{out_day} is
carried but never joined on, so a non-trivial \code{out_day} band counts as a
shadow condition rather than a real constraint.

Sections:
\describe{
\item{\code{shadow_condition}}{\code{decl_yn == 0} rows conditioned on a fact
\code{\link[=match_rule]{match_rule()}} cannot see -- a non-\code{*} declaration/exam attribute
(\code{recover}, \code{recur}, \code{treat}, \code{severe}, \code{cause}, \code{medical_checkup}) or a
constrained \code{out_day} band. The row is silently broader than the author
intended: the engine applies it to inputs the condition was meant to
exclude. Such a row belongs on \code{decl_yn == 1} (or the distinction must move
into a band the feed carries).}
\item{\code{latent_conflict}}{pairs of \code{decl_yn == 0} rules within one \code{kcd_main}
whose bands overlap and whose decisions disagree -- the static form of
\code{\link[=match_rule]{match_rule()}}'s run-time \code{conflict}, found without needing an input to hit
the overlap. A pair that also differs in a shadow condition is flagged
\code{shadow_explained}: the disagreement is legitimate but unresolvable by the
engine (fix via \code{shadow_condition}). A pair that does not is a genuine
structural conflict -- same observable and unobservable conditions, two
answers.}
\item{\code{exact_duplicate}}{\code{decl_yn == 0} rows identical in their bands and
every decision -- redundant rows that inflate \code{\link[=match_rule]{match_rule()}}'s
\code{multi_matched} without changing any outcome.}
\item{\code{no_auto_rule}}{\code{kcd_main} present in the rule set but with no
\code{decl_yn == 0} row, so every input for that disease is \code{unmatched} and
forced to the underwriter -- the static form of \code{\link[=match_rule]{match_rule()}}'s
\code{unmatched}, for diseases the rule set does carry (a disease absent from
the rule set entirely is a mapping gap, not this).}
}
}
\seealso{
\code{\link[=match_rule]{match_rule()}}, \code{\link[=diagnose_icis]{diagnose_icis()}}.
}
