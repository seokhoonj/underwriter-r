% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_si_rule_impact.R
\name{list_si_rule_impact}
\alias{list_si_rule_impact}
\title{List what drives simplified-issue declines, by question and reason}
\usage{
list_si_rule_impact(combined, coverage = NULL)
}
\arguments{
\item{combined}{The per-\verb{(id, coverage)} table from \code{\link[=combine_si_decision]{combine_si_decision()}},
which carries the \code{reason} sheet as its \code{reason_table} attribute -- so the
wording comes from the workbook and no rulebook is passed.}

\item{coverage}{Optional coverage(s) to restrict to; \code{NULL} for all.}
}
\value{
A \code{data.table} of \code{question}, \code{reason}, \code{reason_ko}, \code{n}, \code{share}
(percent of declines), ordered by descending count.
}
\description{
The simplified-issue counterpart of \code{\link[=list_rule_impact]{list_rule_impact()}}. The standard path
measures a rule's marginal lift by relaxing it and re-deciding; SI needs no
counterfactual, because each decline already carries the question and condition
that caused it, so the impact is a direct attribution rather than an estimate.
}
\details{
Counted on the combined table, not on every answer: one insured x coverage is
one decision, and the reason reported is the one that actually decided it.
Counting raw answers would let a cell declined by two questions count twice.
The engine speaks reason keys; the wording (\code{reason_ko}) is joined from the
workbook, so the package source stays ASCII and a rewording is a cell edit.
}
\seealso{
\code{\link[=list_rule_impact]{list_rule_impact()}}, \code{\link[=list_si_decline_disease]{list_si_decline_disease()}}.
}
