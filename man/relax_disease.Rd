% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relax_disease.R
\name{relax_disease}
\alias{relax_disease}
\title{Estimate the auto-rate lift from relaxing a disease}
\usage{
relax_disease(applied, final, kcd_main, mode = c("review_only", "full"))
}
\arguments{
\item{applied}{Per-disease decisions from \code{\link[=match_rule]{match_rule()}} (\verb{$applied}).}

\item{final}{The baseline wide decision table from \code{\link[=combine_decision]{combine_decision()}},
carrying its config-table attributes.}

\item{kcd_main}{The representative disease(s) to relax, e.g. \code{"M51"} or
\code{c("M51", "S33")}.}

\item{mode}{How much to relax a matched target disease: \code{"review_only"}
(default) turns only its manual-review decisions into standard, keeping any
exclusions/loadings/reductions; \code{"full"} turns every decision it makes into
standard. Unmatched targets, which drive manual review everywhere, relax to
standard either way. The reported auto lift is identical for both modes --
exclusions and the like are already auto-decided, so only dropping manual
review raises the auto share -- but \code{"review_only"} is the realistic lever.}
}
\value{
A \code{data.table}, one row per coverage sorted by \code{lift} descending, with
\code{auto_base} (baseline auto share), \code{auto_relaxed} (after relaxing), \code{lift}
(the increase), and \code{n_flipped} (insured moved from manual review to auto).
}
\description{
What-if experiment: if the given representative disease(s) (\code{kcd_main}) were
relaxed to standard (auto-approved, every restriction dropped), how much would
each coverage's auto-decided share rise? The target disease's per-coverage
decisions are set to the standard code, the decisions are re-combined with
\code{\link[=combine_decision]{combine_decision()}}, and the auto share is re-tabulated and compared to the
baseline.
}
\details{
Because a coverage routes to manual review when \emph{any} of an insured's diseases
does, the lift is computed by re-running the full combine -- an insured still
held for review by another disease does not flip.
}
\seealso{
\code{\link[=tabulate_decision]{tabulate_decision()}}, \code{\link[=combine_decision]{combine_decision()}}.
}
