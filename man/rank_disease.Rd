% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_disease.R
\name{rank_disease}
\alias{rank_disease}
\title{Rank diseases by the auto-rate lift of relaxing each}
\usage{
rank_disease(applied, final)
}
\arguments{
\item{applied}{Per-disease decisions from \code{\link[=match_rule]{match_rule()}} (\verb{$applied}).}

\item{final}{The wide decision table from \code{\link[=combine_decision]{combine_decision()}}, carrying its
config-table attributes.}
}
\value{
A \code{data.table}, one row per disease sorted by \code{n_flipped} descending,
with \code{kcd_main}, \code{n_id} (insured moved off review), \code{n_flipped}
(\verb{insured x coverage} cells flipped), and \code{auto_lift} (\code{n_flipped} over all
decision cells).
}
\description{
Which representative disease, if relaxed, moves the most insured off manual
review? For every \code{kcd_main} that drives a coverage to manual review, counts
the insured and the \verb{(insured x coverage)} cells that would flip to auto if it
were relaxed on its own, and ranks them.
}
\details{
A manual-review cell flips only when the relaxed disease is its \emph{sole}
manual-review source (another disease still holding it keeps it on review), so
the lift is exact for relaxing one disease at a time without re-running
\code{\link[=combine_decision]{combine_decision()}} per candidate. Cells already auto-decided -- including
ones a decline outranks -- are not counted. This matches \code{\link[=relax_disease]{relax_disease()}}'s
\code{"review_only"} mode (declines are kept, so the auto share can only rise).
}
\seealso{
\code{\link[=relax_disease]{relax_disease()}} for one disease's per-coverage detail.
}
