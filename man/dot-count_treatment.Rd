% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treatment.R
\name{.count_treatment}
\alias{.count_treatment}
\title{Count hospital days, surgeries, and outpatient visits per group}
\usage{
.count_treatment(dt)
}
\arguments{
\item{dt}{Claim lines already tagged by \code{\link[=.tag_treatment]{.tag_treatment()}}.}
}
\value{
A \code{data.table} keyed on \code{id}, \code{kcd_main} with \code{hos_day}, \code{sur_cnt},
\code{out_cnt}.
}
\description{
\code{hos_day} is the dedup-union of hospital calendar days (\code{\link[instead:count_stay]{instead::count_stay()}}),
not the sum of the per-line figures: overlapping stays must not double-count.
Discharge is clamped to the inquiry date so a stay running past it contributes
only the days already served. \code{sur_cnt} and \code{out_cnt} count distinct accident
dates, so several lines from one episode count once.
}
\details{
Groups with no line of a given type get \code{0}, not \code{NA} -- "treated zero times"
is a fact, and leaving it missing makes every downstream band comparison
propagate \code{NA}.
}
\keyword{internal}
