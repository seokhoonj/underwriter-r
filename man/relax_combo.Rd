% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relax_combo.R
\name{relax_combo}
\alias{relax_combo}
\title{Decompose the joint auto-rate lift of relaxing several diseases together}
\usage{
relax_combo(applied, final, kcd_main, mode = c("review_only", "full"))
}
\arguments{
\item{applied}{Per-disease decisions from \code{\link[=match_rule]{match_rule()}} (\verb{$applied}).}

\item{final}{The baseline wide decision table from \code{\link[=combine_decision]{combine_decision()}}.}

\item{kcd_main}{A character vector of two or more exact representative codes
(e.g. \code{c("M543", "M542", "M13")}) to relax together. Each is matched exactly;
for regex families relax them with \code{\link[=relax_disease]{relax_disease()}} directly.}

\item{mode}{Passed to \code{\link[=relax_disease]{relax_disease()}}: \code{"review_only"} (default) or \code{"full"}.}
}
\value{
A three-row \code{data.table} with \code{component}
(\code{individual}/\code{combined}/\code{synergy}), \code{n_flipped} (insured x coverage cells
moved off manual review), and \code{auto_lift} (\code{n_flipped} over the decision
cells). \code{individual} is the sum of each disease relaxed alone, \code{combined} the
set relaxed at once, \code{synergy} their difference.
}
\description{
Relaxing a set of representative diseases at once lifts the automation rate by
\emph{more} than the sum of relaxing each alone: a cell held on manual review by two
of them flips only when both are relaxed, so it is credited to neither
marginal. This splits the joint effect into the sum of the marginals, the
actual combined lift, and the \code{synergy} between them (the co-held cells the
marginals miss).
}
\details{
The combined and marginal lifts are exact re-combines via \code{\link[=relax_disease]{relax_disease()}}, so
the same \code{mode} applies to both. Under \code{"review_only"} the synergy is always
non-negative (co-holding only). Under \code{"full"} it also absorbs decline
unmasking, so it can be negative -- read it as the interaction, not purely
co-holding.
}
\seealso{
\code{\link[=relax_disease]{relax_disease()}} for one disease's per-coverage detail,
\code{\link[=relax_impact]{relax_impact()}} to rank diseases individually.
}
