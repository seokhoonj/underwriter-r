% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_relax_disease.R
\name{plot_relax_disease}
\alias{plot_relax_disease}
\title{Plot one disease's per-coverage relaxation effect}
\usage{
plot_relax_disease(relaxed, disease = NULL, top = NULL, title = NULL)
}
\arguments{
\item{relaxed}{A \code{data.table} from \code{\link[=relax_disease]{relax_disease()}} (columns \code{coverage},
\code{auto_base}, \code{auto_relaxed}, \code{lift}, \code{n_flipped}).}

\item{disease}{Optional disease label (the \code{kcd_main} passed to
\code{\link[=relax_disease]{relax_disease()}}) woven into the title.}

\item{top}{Optional cap on the number of coverages shown, taken by the largest
absolute lift; default \code{NULL} shows every coverage the disease moves.}

\item{title}{Plot title; by default a sentence naming the disease when
\code{disease} is given, or a generic before/after line otherwise.}
}
\value{
A \code{ggplot} object. Baseline points are grey, relaxed points blue.
}
\description{
Draws the per-coverage before/after from \code{\link[=relax_disease]{relax_disease()}} as a dumbbell chart:
one row per coverage the disease actually moves, a grey point at the baseline
automation rate and a coloured point at the relaxed rate, joined by a segment
whose length is the lift. The lift (percentage points) is labelled at the
relaxed end. Coverages the disease leaves unchanged are dropped.
}
\details{
Complements \code{\link[=plot_relax_impact]{plot_relax_impact()}}: that ranks every disease by its overall
lift, this shows where a single chosen disease's lift lands across coverages.
}
\seealso{
\code{\link[=relax_disease]{relax_disease()}}, \code{\link[=plot_relax_impact]{plot_relax_impact()}}.
}
