% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_decision.R
\name{trace_decision}
\alias{trace_decision}
\title{Trace one insured's final decision back to its per-disease inputs}
\usage{
trace_decision(applied, combined, id)
}
\arguments{
\item{applied}{Per-disease decisions from \code{\link[=match_rule]{match_rule()}} (\verb{$applied}).}

\item{combined}{A wide combined-decision table from \code{\link[=combine_decision]{combine_decision()}},
carrying its config-table attributes.}

\item{id}{The single insured id to trace (must be present in \code{combined}). An id
with no diagnosis in \code{applied} -- an automatic pass recorded by
\code{\link[=combine_decision]{combine_decision()}}'s \code{pass_ids} -- is traced as a pass on every coverage.}
}
\value{
A \code{data.table}, one row per coverage, with columns \code{coverage},
\code{diseases} (the contributing \code{kcd_main:code} inputs, \code{" | "}-separated),
\code{computed} (the decision recomputed for this id), \code{stored} (the value in
\code{combined}), and \code{ok} (\code{computed == stored}). A coverage present on only one
side surfaces as a row with \code{ok = FALSE}.
}
\description{
For a single \code{id}, shows how each coverage's final decision was built from the
per-disease rule decisions in \code{applied}, and verifies it: the decision is
recomputed for that id with \code{\link[=combine_decision]{combine_decision()}} and checked against the
stored \code{combined}. Use it to audit a single case -- what fed in, how it merged,
and whether the stored result is reproducible.
}
\seealso{
\code{\link[=combine_decision]{combine_decision()}}, \code{\link[=tabulate_decision]{tabulate_decision()}}.
}
