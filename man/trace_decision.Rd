% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_decision.R
\name{trace_decision}
\alias{trace_decision}
\title{Trace one insured's final decision back to its per-disease inputs}
\usage{
trace_decision(applied, final, id)
}
\arguments{
\item{applied}{Per-disease decisions from \code{\link[=match_rule]{match_rule()}} (\verb{$applied}).}

\item{final}{A wide final-decision table from \code{\link[=combine_decision]{combine_decision()}}, carrying
its config-table attributes.}

\item{id}{The single insured id to trace.}
}
\value{
A \code{data.table}, one row per coverage that received any per-disease
decision, with columns \code{coverage}, \code{diseases} (the contributing
\code{kcd_main:code} inputs, \code{" | "}-separated), \code{computed} (the decision
recomputed for this id), \code{stored} (the value in \code{final}), and \code{ok}
(\code{computed == stored}).
}
\description{
For a single \code{id}, shows how each coverage's final decision was built from the
per-disease rule decisions in \code{applied}, and verifies it: the decision is
recomputed for that id with \code{\link[=combine_decision]{combine_decision()}} and checked against the
stored \code{final}. Use it to audit a single case -- what fed in, how it merged,
and whether the stored result is reproducible.
}
\seealso{
\code{\link[=combine_decision]{combine_decision()}}, \code{\link[=tabulate_decision]{tabulate_decision()}}.
}
