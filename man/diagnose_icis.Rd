% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_icis.R
\name{diagnose_icis}
\alias{diagnose_icis}
\title{Diagnose the data quality of an ICIS claim table}
\usage{
diagnose_icis(dt, verbose = TRUE)
}
\arguments{
\item{dt}{An ICIS claim table (\code{data.table} or coercible), raw, cleansed, or
mapped by \code{\link[=map_disease]{map_disease()}}.}

\item{verbose}{If \code{TRUE} (default) print a report; the list is always returned
invisibly.}
}
\value{
Invisibly, a named list with \code{n_row}, \code{n_id}, and the sections above.
}
\description{
Reports per-id consistency, row-level anomalies, and a value profile for a raw
or cleansed ICIS feed. Each anomaly is counted as affected rows AND affected
insured, with percentages. Prints a readable report (when \code{verbose}) and
invisibly returns the same numbers as a named list. Aggregates only -- no raw
rows are printed.
}
\details{
Sections:
\describe{
\item{\code{id_consistency}}{insured whose gender, age, or inquiry date vary
across their rows (e.g. multiple ages under one inquiry).}
\item{\code{date_chronology}}{rows violating
\verb{acc_date <= sdate <= edate <= pay_date <= inq_date}.}
\item{\code{row_anomaly}}{hospitalization with no admission date, discharge with
no admission date, date-span vs \code{hos_day} disagreement (by direction),
\code{hos_day < hos_cnt}, duplicate rows.}
\item{\code{hos_day_span}}{among rows with both dates present, how often the
date-span (\code{edate - sdate + 1}) matches \code{hos_day} (percentages within that
base).}
\item{\code{recompute_bound}}{when one endpoint is missing a recompute is forced;
how often each anchor (sdate-forward vs edate-backward) produces an
impossible date, by span direction.}
\item{\code{gender_dist}, \code{age_stat}, \code{missing_required}}{value profile.}
\item{\code{no_diagnosis}}{rows with every \code{kcd} empty -- each still a real claim
-- split by treatment: inpatient/surgery (\code{hos_day}/\code{sur_cnt}/\code{hos_cnt} >
0), outpatient (a visit date only), or truly empty (no treatment and no
date). Plus the count of insured with no coded row at all. \code{\link[=clean_icis]{clean_icis()}}
keeps these rows on the no-diagnosis code, so none is lost.}
\item{\code{scope}}{only on a table from \code{\link[=map_disease]{map_disease()}}, which carries the
lookback flags: diagnoses no window covers, and the insured left with
nothing in scope to underwrite, split into those who never had a coded row
and those whose every diagnosis aged out. A claim-level feed cannot show
this -- the windows live in the disease table.}
}
}
