% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kcd.R
\name{normalize_kcd}
\alias{normalize_kcd}
\title{Normalize a diagnosis-code cell to the flat KCD form}
\usage{
normalize_kcd(x, len = 4L)
}
\arguments{
\item{x}{A character vector of raw diagnosis-code cells.}

\item{len}{Integer length to roll each flat code to (default \code{4L}).}
}
\value{
A character vector of normalized codes; entries that do not match the
KCD shape become \code{NA}.
}
\description{
Denoise then parse: uppercase and keep only code characters (\verb{[A-Z0-9]}, dot,
comma), take the first comma-separated code, drop the dot, roll to \code{len}
characters, and validate the KCD shape (one letter followed by two or more
digits). Fully vectorized, so it runs at data.table speed over millions of
rows and returns the first code of each cell.
}
\examples{
normalize_kcd(c("m00.0", "M51.13", "K63.5, S33"))
}
\seealso{
\code{\link[=split_kcd]{split_kcd()}} to keep every code in a cell.
}
