% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_icis.R
\name{clean_icis}
\alias{clean_icis}
\title{Cleanse a raw ICIS claim table into a wide master}
\usage{
clean_icis(
  dt,
  kcd_cols = paste0("kcd", 0:4),
  method = c("sdate", "edate", "auto")
)
}
\arguments{
\item{dt}{A raw ICIS claim table (\code{data.table} or coercible). Expected columns
include \code{id}, \code{gender}, \code{age}, \code{inq_date}, \code{pay_date}, \code{acc_date}, \code{sdate},
\code{edate}, \code{hos_day}, \code{hos_cnt}, \code{sur_cnt}, and the diagnosis columns; dates
are \code{YYYYMMDD} strings.}

\item{kcd_cols}{Character vector of diagnosis-code column names
(default \code{kcd0..kcd4}).}

\item{method}{Endpoint-reconciliation basis; one of \code{"sdate"} (default),
\code{"edate"}, \code{"auto"}.}
}
\value{
A cleansed wide \code{data.table}, one row per (deduplicated) claim line.
}
\description{
One row per claim line: keep rows carrying a diagnosis code, parse the dates,
reconcile the admission/discharge window through \code{hos_day}, normalize the
diagnosis codes, pull them leftward (so \code{kcd0} is the main diagnosis), and
drop exact duplicate rows.
}
\details{
\code{method} picks which admission/discharge endpoint is trusted and which is
derived from \code{hos_day}:
\describe{
\item{\code{"sdate"}}{trust admission; discharge = \code{sdate + hos_day - 1}.}
\item{\code{"edate"}}{trust discharge; admission = \code{edate - hos_day + 1}.}
\item{\code{"auto"}}{per row prefer the sdate basis, but if its derived discharge
would fall after pay/inquiry (an ordering violation) switch to the edate
basis, and if that derived admission would fall before the accident date
fall back to the sdate basis.}
}
With only one raw endpoint the anchor is forced to it. \code{hos_day == 0} is an
outpatient visit (no stay): \code{sdate} is its visit date and is kept; \code{edate}
stays \code{NA}. A large \code{hos_day} can push the derived endpoint past the inquiry
date (still hospitalized); that is left visible and handled at aggregation.
}
\seealso{
\code{\link[=filter_latest_inquiry]{filter_latest_inquiry()}}, \code{\link[=melt_kcd]{melt_kcd()}}.
}
