% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_decline_disease.R
\name{list_decline_disease}
\alias{list_decline_disease}
\title{Rank the diseases behind automated declines, by coverage}
\usage{
list_decline_disease(applied, coverage = NULL, n_top = 20L)
}
\arguments{
\item{applied}{Per-disease decisions from \code{\link[=match_rule]{match_rule()}} (\verb{$applied}), carrying
its \code{decision_cols} attribute.}

\item{coverage}{Optional coverage(s) to restrict to; \code{NULL} for all.}

\item{n_top}{How many diseases to return.}
}
\value{
A \code{data.table} of \code{kcd_main}, \code{n} (contribution count), \code{share}
(percent of contributions), ranked by descending count, with a
\code{decline_cells} attribute holding the true number of declined cells.
}
\description{
For every representative disease (\code{kcd_main}) that decides \code{D} on a coverage,
the number of \verb{(insured x coverage)} cells it drives to an automated decline.
Where \code{\link[=list_rule_impact]{list_rule_impact()}} asks "which referral would relaxing a rule flip",
this asks the plainer question the decline analysis needs: which diseases
produce the declines that already stand.
}
\details{
\code{D} outranks every other code in the combiner, so a disease that decides \code{D}
on a coverage decides the insured's final decision there. Counts are per
\verb{(insured x disease x coverage)} contribution: one declined cell can be driven
by several diseases at once, so these contributions sum to MORE than the
declined-cell count. That is deliberate -- the question is "which diseases
cause declines", not "how many cells declined" -- but it means \code{share} is a
share of contributions, not of cells. The declined-cell total is returned as
the \code{decline_cells} attribute for an honest denominator.
}
\seealso{
\code{\link[=list_rule_impact]{list_rule_impact()}} for the relax-to-flip counterfactual,
\code{\link[=tabulate_decision]{tabulate_decision()}} for the decision distribution.
}
