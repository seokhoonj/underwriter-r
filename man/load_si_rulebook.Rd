% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_si_rulebook.R
\name{load_si_rulebook}
\alias{load_si_rulebook}
\title{Read a simplified-issue rulebook workbook}
\usage{
load_si_rulebook(path)
}
\arguments{
\item{path}{Path to the rulebook workbook (\code{.xlsx}).}
}
\value{
A named list with the sheet tables \code{product}, \code{coverage}, \code{decision},
\code{ruleset}, \code{sentinel}, \code{critical_disease}, \code{reason}, plus the derived
lookups \code{code} (role -> letter), \code{rank} (letter -> priority) and \code{auto}
(letter -> 0/1).
}
\description{
Reads the seven sheets of a simplified-issue rulebook and returns them as one
validated list, the counterpart of the standard \code{\link[=combine_ruleset]{combine_ruleset()}} input. The
workbook is assembled offline from the delivered rule sheets; this function
only reads and checks.
}
\details{
Besides the sheets it adds three derived lookups the engine relies on: \code{code}
(role -> code letter), \code{rank} (code letter -> priority, lower is worse), and
\code{auto} (code letter -> 1 automated / 0 referred). Validation fails loudly at
the boundary rather than deep in the engine: a duplicated band key, a missing
role, a reason key with no wording, a \code{priority} that does not order decline
worst, or a \code{ruleset_sentinel} that is missing or repeats a reserved sentinel
code (the engine assigns \code{EXPIRED} and settles all four by role) all stop here,
where the cause is visible.
}
\seealso{
\code{\link[=si_product]{si_product()}}, \code{\link[=match_si_rule]{match_si_rule()}}, \code{\link[=combine_si_decision]{combine_si_decision()}}.
}
