% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_si_rule.R
\name{match_si_rule}
\alias{match_si_rule}
\title{Match mapped claim lines against a simplified-issue rule set}
\usage{
match_si_rule(mapped, rulebook, product, source = c("icis", "declaration"))
}
\arguments{
\item{mapped}{Mapped claim lines from \code{\link[=map_disease]{map_disease()}}.}

\item{rulebook}{A rulebook from \code{\link[=load_si_rulebook]{load_si_rulebook()}}.}

\item{product}{Product configuration from \code{\link[=si_product]{si_product()}}.}

\item{source}{\code{"icis"} (claim history alone) or \code{"declaration"}. Q1's recent
history declines on claim data but only refers on a declaration, which
carries context ICIS cannot show (icis_spec 2.702 rows 138-145).}
}
\value{
A \code{data.table}, one row per answer, with \code{question}
(\code{Q1}/\code{Q2}/\code{Q3}/\code{sentinel}, or \code{NA} for an insured that tripped nothing),
\code{id}, \code{coverage}, \code{kcd_main}, \code{dec}, \code{reason}.
}
\description{
The simplified-issue counterpart of \code{\link[=match_rule]{match_rule()}}. Where the standard path
matches one band per disease, this evaluates the three application-form
questions independently and returns every answer they produced;
\code{\link[=combine_si_decision]{combine_si_decision()}} folds them per coverage. Because each question windows
and counts differently there is no shared aggregation step -- SI has no
\code{aggregate_disease} counterpart -- so this reads \code{mapped} directly.
}
\details{
The four reserved sentinel codes (no diagnosis, all aged out, unreadable, not
in the mapping table) are structural states, not diseases; putting them to the
questions would decline an applicant for having no claim history. They are
withheld from the questions and settled from the \code{ruleset_sentinel} sheet.

Every insured is represented in the result: one who tripped no question is
carried through as an explicit standard on every coverage (a \code{question}-\code{NA}
row), not omitted. The result is therefore the whole roster, so
\code{\link[=combine_si_decision]{combine_si_decision()}} folds it without a separate id list.
}
\seealso{
\code{\link[=combine_si_decision]{combine_si_decision()}}, \code{\link[=si_product]{si_product()}}, \code{\link[=load_si_rulebook]{load_si_rulebook()}}.
}
