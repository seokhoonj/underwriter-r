% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_ruleset.R
\name{combine_ruleset}
\alias{combine_ruleset}
\title{Assemble the engine's rule set from disease rules and sentinel catch-alls}
\usage{
combine_ruleset(ruleset, sentinel)
}
\arguments{
\item{ruleset}{The disease rule table, one row per rule, carrying an integer
\code{no}.}

\item{sentinel}{The sentinel catch-all rows, the same columns as \code{ruleset}
except \code{no} (assigned here).}
}
\value{
A single \code{data.table}: \code{ruleset} with \code{sentinel} appended below it and
renumbered. Column order follows \code{ruleset} (so \code{no} stays first).
}
\description{
\code{\link[=match_rule]{match_rule()}} consumes one rule-set table, but the four sentinel catch-all
rows (\code{"VACANT"}, \code{"EXPIRED"}, \code{"IRREGULAR"}, \code{"UNMAPPED"}) are authored apart
from the disease rules -- kept in their own place so re-authoring the disease
rules cannot silently drop them. This appends \code{sentinel} below \code{ruleset} and
continues the rule number as \verb{max(ruleset$no) + 1, 2, ...}, so a \code{no} is never
duplicated and the sentinels always sit after however large the disease rule
set has grown. \code{sentinel} carries no \code{no} of its own; it is assigned here (a
stale \code{no} it happens to carry is overwritten, not trusted).
}
\seealso{
\code{\link[=match_rule]{match_rule()}}, \code{\link[=diagnose_ruleset]{diagnose_ruleset()}}.
}
