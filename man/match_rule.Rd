% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_rule.R
\name{match_rule}
\alias{match_rule}
\title{Match aggregated inputs against a rule set}
\usage{
match_rule(aggregated, rule)
}
\arguments{
\item{aggregated}{Per-\verb{(id, kcd_main)} inputs from \code{\link[=aggregate_disease]{aggregate_disease()}},
carrying \code{age}.}

\item{rule}{A rule-set \code{data.table}.}
}
\value{
A list with \code{applied} (the input plus \code{matched}, \code{conflict}, and one
decision column per product; the decision-column names are stored on the
\code{"decision_cols"} attribute), \code{decision_cols}, \code{unmatched} (the input rows
no rule matched), \code{multi_matched} (each multi-matched input joined to the
overlapping rules it hit, for rule-set cleanup), \code{conflict} (the subset of
\code{multi_matched} whose matched rules disagree on a decision), and three
diagnostic counts: \code{n_unmatched} (inputs no rule matched), \code{n_multi_matched}
(inputs matched by more than one rule), and \code{n_conflict} (inputs whose
matched rules disagree, so \code{n_conflict <= n_multi_matched}).
}
\description{
For each \verb{(id, kcd_main)} input, finds the rule row whose bands it falls in
and attaches that rule's per-product decisions. Matching is a non-equi band
join: \code{kcd_main} equality plus \code{age}, \code{elp_day}, \code{sur_cnt}, \code{hos_day} bands.
\code{out_day} is left unconstrained in the rule set, so \code{out_cnt} is not matched
on. Only no-declaration rules (\code{decl_yn == 0}) are used -- declaration-
dependent rows hinge on facts the claim feed does not carry. A disease with no
matching rule is kept with \code{matched == 0} so it can route to manual review.
}
\details{
Near-all multi-matches are identical duplicates; the lowest-\code{ord} match is
kept and the few genuine conflicts are flagged in \code{conflict}. The rule set is
expected to follow a fixed schema: keys, band bounds, and declaration
attributes (the non-decision columns), plus one decision column per product.
}
\seealso{
\code{\link[=combine_decision]{combine_decision()}}.
}
