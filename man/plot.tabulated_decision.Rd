% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-decision.R
\name{plot.tabulated_decision}
\alias{plot.tabulated_decision}
\title{Plot the decision composition per coverage}
\usage{
\method{plot}{tabulated_decision}(
  x,
  ...,
  group = c("auto", "category"),
  order = c("auto_high", "auto_low", "column"),
  min_label = 0.03,
  title = "Decision composition per coverage"
)
}
\arguments{
\item{x}{A \code{\link[=tabulate_decision]{tabulate_decision()}} result (class \code{tabulated_decision}).}

\item{...}{Unused.}

\item{group}{Column to stack and colour by: \code{"auto"} (default) or
\code{"category"}.}

\item{order}{Coverage order along the x-axis: \code{"auto_high"} (default, highest
auto-decided share first), \code{"auto_low"} (lowest first), or \code{"column"} (the
coverage column order defined in the rule set, from the \code{decision_cols}
attribute the tabulation carries).}

\item{min_label}{Segments whose share is at or below this are left unlabelled,
to keep thin slivers from cluttering (default \code{0.03}).}

\item{title}{Plot title (default \code{"Decision composition per coverage"}).}
}
\value{
A \code{ggplot} object. When \code{group = "auto"}, auto-decided (\code{1}) is blue
and underwriter-referred (\code{0}) is red.
}
\description{
\code{plot()} method for a \code{\link[=tabulate_decision]{tabulate_decision()}} result (class \code{tabulated_decision}):
a stacked bar of each coverage's decision composition -- by default the
auto-decided vs underwriter-referred share -- with each segment's percentage labelled
at its centre.
}
\seealso{
\code{\link[=tabulate_decision]{tabulate_decision()}}, \code{\link[=combine_decision]{combine_decision()}}.
}
