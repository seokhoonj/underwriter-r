% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_si_decision.R
\name{combine_si_decision}
\alias{combine_si_decision}
\title{Combine the three questions' answers into one decision per coverage}
\usage{
combine_si_decision(matched, rulebook, product)
}
\arguments{
\item{matched}{Answers from \code{\link[=match_si_rule]{match_si_rule()}}, carrying every insured.}

\item{rulebook}{A rulebook from \code{\link[=load_si_rulebook]{load_si_rulebook()}}.}

\item{product}{Product configuration from \code{\link[=si_product]{si_product()}}.}
}
\value{
A \code{data.table}, one row per \verb{(id, coverage)}: \code{dec}, the \code{reason} that
produced it, the \code{question} it came from, and the driving \code{kcd_main}. The
\code{decision} and \code{reason} sheets ride along as the \code{decision_table} and
\code{reason_table} attributes, so \code{\link[=tabulate_si_decision]{tabulate_si_decision()}} and
\code{\link[=list_si_rule_impact]{list_si_rule_impact()}} read them without a rulebook argument.
}
\description{
The simplified-issue counterpart of \code{\link[=combine_decision]{combine_decision()}}. Where the standard
path composes eight codes through per-code combiners, this takes the worst of
three by the \code{decision} sheet's \code{priority} (smaller is worse, so decline wins).
Every insured x coverage the product sells gets a row: \code{\link[=match_si_rule]{match_si_rule()}} already
carries the whole roster, so the id universe is read off \code{matched} -- the same
no-insured-left-behind invariant the standard path holds through its sentinels,
with no separate id list to pass. Presence rows (no question, no decision) seed
the roster only; a grid cell left with no answer is the baseline standard, a
no-applicable-condition accept.
}
\seealso{
\code{\link[=match_si_rule]{match_si_rule()}}, \code{\link[=tabulate_si_decision]{tabulate_si_decision()}}.
}
