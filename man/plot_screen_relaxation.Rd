% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_screen_relaxation.R
\name{plot_screen_relaxation}
\alias{plot_screen_relaxation}
\title{Plot the disease relaxation ranking}
\usage{
plot_screen_relaxation(ranking, top = 12L, fill = "#4E79A7", title = NULL)
}
\arguments{
\item{ranking}{A \code{data.table} from \code{\link[=screen_relaxation]{screen_relaxation()}}. If it carries a \code{coverage}
column it must hold a single coverage -- slice one first, e.g.
\code{ranking[coverage == "adb"]}.}

\item{top}{Number of top-ranked diseases to show (default \code{12}).}

\item{fill}{Bar fill colour (default \code{"#4E79A7"}).}

\item{title}{Plot title; by default a sentence naming the coverage for a
single-coverage slice, or the overall gain otherwise.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the ranking from \code{\link[=screen_relaxation]{screen_relaxation()}} as a horizontal bar chart: one bar per
representative disease, its length the automation-rate lift (percentage points)
from relaxing that disease on its own, labelled with the lift and the insured
count moved off manual review. Pass either the overall ranking
(\code{screen_relaxation(applied, final)}) or a single coverage's ranking (one coverage
sliced out of \code{screen_relaxation(..., by_coverage = TRUE)}); when a \code{coverage}
column is present its name is woven into the axis label and title.
}
\seealso{
\code{\link[=screen_relaxation]{screen_relaxation()}}, \code{\link[=plot_decision]{plot_decision()}}.
}
